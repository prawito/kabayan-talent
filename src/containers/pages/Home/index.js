import React, { Fragment } from 'react';
import { SearchBar } from '../../../components/atoms';

const Home = () => {
  return (
    <Fragment>
      <SearchBar />
      <p>Home Page</p>
    </Fragment>
  );
};

export default Home;
